# Reversa — Project Configuration
# This file is managed by the installer. For customizations, edit config.user.toml

[project]
name = ""
version = "1.0.0"

[user]
name = ""
chat_language = "en"
doc_language = "en"

[output]
folder = "_reversa_sdd"

[agents]
installed = [
  "reversa-scout",
  "reversa-archaeologist",
  "reversa-detective",
  "reversa-architect",
  "reversa-writer",
  "reversa-reviewer",
  "reversa-visor",
  "reversa-data-master",
  "reversa-design-system",
  "reversa-extract-soul"
]

[engines]
installed = []

[analysis]
answer_mode = "chat"
doc_level = "complete"

# Organization of the specs generated by the agents.
# This section is filled by Reversa after the Scout suggests a structure
# and the user confirms the choice in the menu (right after the doc_level question).
# To re-display the menu, remove the entire [specs] section and run Reversa again.
[specs]
layout = ""              # constant "feature-folder" after the first execution
granularity = ""         # module | use-case | endpoint | hybrid | feature | custom
custom_folders = []      # filled only when granularity == "custom"
scout_suggestion = ""    # record of the original Scout suggestion, immutable after the 1st execution
decided_at = ""          # ISO 8601 timestamp of the user's choice
